# Phase 6 Completion Summary — Gloss Conversion via Ollama (Step 12, Model 3, Fallback E)

Files created: backend/gloss/prompts.py (SYSTEM_PROMPT and FEW_SHOT_EXAMPLES, copied from AI_Model_Specification_Document.docx's Model 3 section; verified programmatically byte-for-byte against the source document, differing only in markdown's own hard-line-break trailing spaces, which are formatting artifacts, not content); backend/gloss/vocab_filter.py (filter_gloss(), case-insensitive allowed_vocab membership check, silently drops and logs anything else, never raises); backend/logs/ollama_raw_log.py (append-only JSONL log at backend/logs/ollama_raw_log.jsonl, gitignored as generated runtime data); backend/gloss/ollama_client.py completed (warm_up() from Phase 3 retained; added _get_allowed_vocab() built once from dictionary_loader and cislr_index keys, and run_cycle(session_id), the full Step 12 request/response/filter/commit-advance cycle).

Cross-boundary signature: ollama_client.run_cycle(session_id) -> list[str] | None, called per-session per cycle; returns validated gloss words or None if the cycle was skipped (empty stable_words) or discarded (Fallback E). render_resolver.py (Phase 7) is not called yet.

Key data structures: the Ollama request payload {stable_words, provisional_context, allowed_vocab} and response {"gloss": [...]} match the frozen shapes exactly. The prompt sent to Ollama is constructed as system=SYSTEM_PROMPT (Ollama's native system field) and prompt=FEW_SHOT_EXAMPLES + "\n\nInput: <json>\nOutput:" - this field mapping is a decision, not specified in the docs, made to keep the system prompt and few-shot examples cleanly separated while matching the docs' own "few-shot appended after system prompt, before each real request" structure.

No ambiguities beyond the field-mapping decision above.

Human step required, not simulated: the phase document explicitly calls this file's exact wording the single highest-consequence exact-text dependency in the project and requires human review before treating it as final, even though I have already verified it programmatically. Please open backend/gloss/prompts.py alongside docs/AI_Model_Specification_Document.docx.md (the "Exact system prompt" and "Exact few-shot examples" sections) and confirm no wording was altered, then confirm back so Phase 7 can begin.

Verified: ran the full cycle against the real Ollama service and the real dictionary/CISLR vocabulary (a short developer-typed sentence, used only to exercise the code path, not stored as a fixture and deleted from the log afterward). The model returned "I", which is not an exact match in the real vocabulary (the dictionary's gloss is "I/ME"), and vocab_filter correctly dropped it while GO and SCHOOL passed through - directly demonstrating why the mandatory filter matters regardless of prompt compliance. Also verified malformed and wrong-shape responses are discarded with the buffer left untouched. Re-ran the full backend; /health still reports all five components ready.
